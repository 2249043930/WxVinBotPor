# WxVinBot Por 前端开发提示词

## 一、项目概述

WxVinBot Por 是一个基于微信群的汽车配件询价管理系统，前端需要实现用户管理、数据看板、车型配置、厂群管理、数据统计等核心功能模块。

---

## 二、技术栈要求

| 技术项 | 要求 |
|--------|------|
| 前端框架 | Vue 3 + TypeScript |
| UI组件库 | Element Plus |
| CSS方案 | 本地CSS文件（禁止外链） |
| HTTP客户端 | Axios |
| 构建工具 | Vite |
| 状态管理 | Pinia |
| 路由管理 | Vue Router 4 |

---

## 三、项目目录结构

```
src/
├── api/                    # API接口定义
│   ├── auth.ts            # 认证相关接口
│   ├── dashboard.ts       # 数据看板接口
│   ├── user.ts            # 用户管理接口
│   ├── carModel.ts        # 车型配置接口
│   ├── group.ts           # 厂群管理接口
│   ├── statistics.ts      # 数据统计接口
│   └── log.ts             # 日志管理接口
├── assets/                # 静态资源
│   ├── images/
│   └── icons/
├── components/            # 公共组件
│   ├── common/           # 通用组件
│   │   ├── Pagination.vue    # 分页组件
│   │   ├── SearchForm.vue    # 搜索表单
│   │   ├── DataTable.vue     # 数据表格
│   │   ├── ChartCard.vue     # 图表卡片
│   │   └── StatCard.vue      # 统计卡片
│   ├── charts/           # 图表组件
│   │   ├── PieChart.vue
│   │   ├── BarChart.vue
│   │   └── LineChart.vue
│   └── layout/           # 布局组件
│       ├── Sidebar.vue
│       ├── Header.vue
│       ├── Breadcrumb.vue
│       └── MainContent.vue
├── views/                 # 页面视图
│   ├── login/            # 登录页面
│   ├── dashboard/        # 首页数据看板
│   ├── system/           # 系统管理
│   │   ├── LLMConfig.vue
│   │   ├── UserManage.vue
│   │   ├── LogManage.vue
│   │   ├── WechatManage.vue
│   │   └── AccountManage.vue
│   ├── carModel/         # 车型配置
│   │   └── CarModelConfig.vue
│   ├── group/            # 厂群管理
│   │   ├── GroupManage.vue
│   │   └── VinRecord.vue
│   └── statistics/       # 数据统计
│       └── MessageRecord.vue
├── router/                # 路由配置
│   └── index.ts
├── stores/                # Pinia状态管理
│   ├── user.ts
│   ├── app.ts
│   └── permission.ts
├── styles/                # 样式文件（本地CSS）
│   ├── variables.css     # CSS变量
│   ├── common.css        # 公共样式
│   ├── layout.css        # 布局样式
│   ├── components.css    # 组件样式
│   └── views/            # 页面样式
│       ├── login.css
│       ├── dashboard.css
│       ├── system.css
│       ├── carModel.css
│       ├── group.css
│       └── statistics.css
├── utils/                 # 工具函数
│   ├── request.ts        # Axios封装
│   ├── auth.ts           # 认证工具
│   ├── format.ts         # 格式化工具
│   └── validate.ts       # 验证工具
├── types/                 # TypeScript类型定义
│   ├── user.ts
│   ├── dashboard.ts
│   ├── carModel.ts
│   ├── group.ts
│   └── common.ts
├── App.vue
└── main.ts
```

---

## 四、页面详细设计

### 4.1 登录页面 (/login)

#### 页面布局
- 左侧：品牌展示区域（占50%），深色背景，展示系统Logo和简介
- 右侧：登录表单区域（占50%），白色背景，居中显示

#### 表单字段
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 登录账号 | Input | 是 | 支持用户名/手机号/邮箱 |
| 用户密码 | Password | 是 | 至少6位，支持显示/隐藏切换 |
| 记住密码 | Checkbox | 否 | 本地存储登录信息 |
| 验证码 | Input | 是 | 图形验证码，点击刷新 |

#### 交互逻辑
- 表单验证：实时验证，错误提示
- 登录按钮：加载状态，防止重复提交
- 登录成功：存储Token，跳转首页
- 登录失败：显示错误信息，清空验证码

#### 样式要求
- 整体采用简约商务风格
- 表单卡片带轻微阴影和圆角
- 输入框聚焦时有蓝色边框高亮
- 按钮使用渐变色背景

---

### 4.2 首页数据看板 (/dashboard)

#### 页面布局
- 顶部：关键指标卡片行（4-6个统计卡片）
- 中部：图表区域（左右两栏布局）
- 底部：数据列表（热门车型排行等）

#### 关键指标卡片
| 指标名称 | 数据来源 | 显示格式 |
|----------|----------|----------|
| 数据库车辆总数 | 统计API | 数字，带增长箭头 |
| 本月新增 | 统计API | 数字，环比变化 |
| 本周新增 | 统计API | 数字，环比变化 |
| 今日新增 | 统计API | 数字 |
| 群聊数量 | 统计API | 数字 |
| LLM识别成功率 | 统计API | 百分比进度条 |

#### 图表组件
1. **车辆类型统计饼图**
   - 展示各类型车辆占比
   - 支持图例切换显示/隐藏
   - 悬停显示详细数据

2. **热门车型排行柱状图**
   - Top 10车型排行
   - 横向柱状图展示
   - 支持排序切换

3. **排量分布统计图**
   - 各排量区间分布
   - 柱状图或饼图展示

4. **新增趋势折线图**
   - 近30天新增车辆趋势
   - 支持时间范围筛选

#### 数据列表
- 热门车型排行表格
- 最近识别记录表格
- 支持分页和排序

---

### 4.3 系统管理模块

#### 4.3.1 LLM配置 (/system/llm)

##### 页面布局
- 表单卡片布局，分为基础配置和提示词配置两大部分

##### 配置项
| 配置项 | 类型 | 说明 |
|--------|------|------|
| API Key | Input | LLM服务API密钥，支持显示/隐藏 |
| 模型选择 | Select | 下拉选择可用模型 |
| 提示词设置 | Textarea | 车架号识别提示词模板 |
| 温度参数 | Slider | 0-1范围调节 |
| 最大Token | Input Number | 响应长度限制 |

##### 提示词模板变量
- `{image}`: 图片内容占位
- `{vin}`: 车架号占位
- 支持变量插入快捷按钮

#### 4.3.2 用户管理 (/system/user)

##### 页面布局
- 顶部：搜索栏 + 操作按钮（添加用户）
- 中部：用户列表表格
- 底部：分页组件

##### 表格字段
| 字段 | 说明 | 操作 |
|------|------|------|
| 用户昵称 | 显示名称 | - |
| 登录账号 | 用户名 | - |
| 手机号码 | 联系方式 | - |
| 邮箱账号 | 邮箱地址 | - |
| 备注 | 用户备注 | - |
| 创建时间 | 注册时间 | - |
| 状态 | 启用/禁用 | 可切换 |
| 操作 | - | 编辑、删除、重置密码 |

##### 添加/编辑用户弹窗
| 字段 | 类型 | 必填 | 验证规则 |
|------|------|------|----------|
| 用户昵称 | Input | 是 | 2-20字符 |
| 登录账号 | Input | 是 | 4-20字符，字母数字下划线 |
| 用户密码 | Password | 添加时必填 | 6-20字符 |
| 手机号码 | Input | 否 | 手机号格式 |
| 邮箱账号 | Input | 否 | 邮箱格式 |
| 备注 | Textarea | 否 | 最多200字 |

#### 4.3.3 日志管理 (/system/log)

##### 页面布局
- 顶部：筛选条件（日期范围、用户、IP）
- 中部：日志列表表格
- 底部：分页组件

##### 表格字段
| 字段 | 说明 |
|------|------|
| 序号 | 自增序号 |
| 用户昵称 | 操作用户 |
| IP地址 | 访问IP |
| 登录地点 | IP归属地 |
| 操作系统 | 用户系统信息 |
| 浏览器 | 浏览器类型 |
| 登录时间 | 精确到秒 |
| 操作内容 | 操作描述 |

##### 筛选条件
- 日期范围选择器
- 用户下拉选择
- IP地址输入框
- 操作类型选择

#### 4.3.4 微信管理 (/system/wechat)

##### 页面布局
- 微信账号绑定列表
- 每个账号显示Wx_id号
- 支持解绑和重新绑定

##### 功能
- 显示已绑定的微信账号
- 展示Wx_id号
- 绑定状态显示
- 解绑操作（需确认）

#### 4.3.5 账户管理 (/system/account)

##### 页面布局
- 消费记录表格
- 统计卡片展示

##### 统计卡片
| 指标 | 说明 |
|------|------|
| LLM识别成功次数 | 累计成功次数 |
| LLM识别失败次数 | 累计失败次数 |
| LLM识别成功率 | 百分比展示 |

##### 消费记录表格
| 字段 | 说明 |
|------|------|
| 序号 | 自增序号 |
| 日期 | 消费日期 |
| 识别次数 | 当日识别次数 |
| 成功次数 | 成功识别数 |
| 失败次数 | 失败识别数 |
| 成功率 | 当日成功率 |

---

### 4.4 车型配置 (/carModel/config)

#### 页面布局
- Tab切换：成员类型配置 / 经营车型配置

#### 成员类型配置Tab
##### 成员类型列表
| 类型 | 说明 |
|------|------|
| 客户 | 普通客户 |
| 业务员 | 销售人员 |
| 客服 | 客服人员 |

##### 功能
- 添加成员类型
- 编辑类型名称
- 删除类型（需确认）
- 设置类型权限

#### 经营车型配置Tab
##### 汽配商管理
| 字段 | 说明 |
|------|------|
| 汽配商wxid | 微信ID |
| 汽配商名称 | 显示名称 |
| 可选车型 | 该商家可经营的车型列表 |
| 已选车型 | 已配置的车型 |

##### 车型选择器
- 树形结构展示车型
- 支持搜索筛选
- 多选功能
- 已选车型标签展示

---

### 4.5 厂群管理模块

#### 4.5.1 群管理 (/group/manage)

##### 页面布局
- 顶部：搜索栏 + 添加群按钮
- 中部：群列表表格
- 底部：分页组件

##### 表格字段
| 字段 | 说明 | 操作 |
|------|------|------|
| 序号 | 自增序号 | - |
| 群名称 | 微信群名称 | - |
| 群ID | 微信群ID | - |
| 成员数 | 群内人数 | - |
| 状态 | 启用/禁用 | 可切换 |
| 创建时间 | 添加时间 | - |
| 操作 | - | 编辑、删除、配置通知 |

##### 通知配置弹窗
| 通知类型 | 开关 | 说明 |
|----------|------|------|
| 询价通知 | Switch | 收到询价时通知 |
| 报价通知 | Switch | 收到报价时通知 |
| 发货通知 | Switch | 发货时通知 |
| 成交通知 | Switch | 成交时通知 |
| 工作时间 | Time Range | 通知生效时间段 |

#### 4.5.2 车架号记录 (/group/vin)

##### 页面布局
- 顶部：高级筛选栏
- 中部：记录列表表格
- 底部：分页组件

##### 表格字段
| 字段 | 说明 | 可筛选 |
|------|------|--------|
| 序号 | 自增序号 | - |
| 日期 | 识别日期 | 日期范围 |
| 识别状态 | 成功/失败 | 下拉选择 |
| 车架号 | VIN码 | 输入框 |
| 车型 | 识别出的车型 | 输入框 |
| 来源群 | 微信群名称 | 下拉选择 |
| 询价人 | 发送询价的人 | 输入框 |
| 报价人 | 回复报价的人 | 输入框 |
| 是否报价 | 是/否 | 下拉选择 |
| 报价时效 | 报价响应时间 | - |
| 汽配商 | 关联汽配商 | 下拉选择 |
| 操作 | - | 查看详情、删除 |

##### 筛选条件
- 日期范围选择器
- 识别状态下拉框
- 来源群下拉框
- 车架号输入框
- 车型输入框
- 是否报价下拉框
- 汽配商下拉框

##### 详情弹窗
- 完整识别信息展示
- 原始图片预览
- 识别结果JSON展示
- 相关聊天记录

---

### 4.6 数据统计 (/statistics/message)

#### 页面布局
- 顶部：筛选条件
- 中部：消息记录表格
- 底部：分页组件

#### 表格字段
| 字段 | 说明 | 可筛选 |
|------|------|--------|
| 序号 | 自增序号 | - |
| 消息类型 | 文本/图片/语音 | 下拉选择 |
| 图片内容 | 缩略图展示 | - |
| 日期 | 消息时间 | 日期范围 |
| 询价人 | 发送者 | 输入框 |
| 车架号 | 识别的VIN | 输入框 |
| 车型 | 识别结果 | 输入框 |
| 来源群 | 微信群 | 下拉选择 |
| 操作 | - | 查看详情 |

#### 筛选条件
- 日期范围选择器
- 消息类型下拉框
- 来源群下拉框
- 询价人输入框

---

## 五、组件设计规范

### 5.1 公共组件

#### Pagination 分页组件
```vue
<Pagination 
  :total="total" 
  :page="currentPage" 
  :limit="pageSize"
  @pagination="handlePagination" 
/>
```

#### SearchForm 搜索表单
```vue
<SearchForm 
  :fields="searchFields" 
  @search="handleSearch"
  @reset="handleReset"
/>
```

#### DataTable 数据表格
```vue
<DataTable 
  :columns="columns"
  :data="tableData"
  :loading="loading"
  @sort-change="handleSort"
/>
```

### 5.2 图表组件

使用 ECharts 5.x，封装为 Vue 组件：
- 支持响应式 resize
- 支持主题切换
- 支持数据更新动画

---

## 六、API接口规范

### 6.1 响应格式
```typescript
interface ApiResponse<T> {
  code: number;      // 0为成功，其他为错误码
  message: string;   // 提示信息
  data: T;          // 响应数据
}
```

### 6.2 分页参数
```typescript
interface PaginationParams {
  page: number;      // 当前页码
  pageSize: number;  // 每页条数
}

interface PaginationData<T> {
  list: T[];         // 数据列表
  total: number;     // 总条数
  page: number;      // 当前页码
  pageSize: number;  // 每页条数
}
```

### 6.3 主要接口列表

| 接口路径 | 方法 | 说明 |
|----------|------|------|
| /api/auth/login | POST | 用户登录 |
| /api/auth/logout | POST | 用户登出 |
| /api/auth/info | GET | 获取当前用户信息 |
| /api/dashboard/stats | GET | 获取看板统计数据 |
| /api/dashboard/charts | GET | 获取图表数据 |
| /api/user/list | GET | 获取用户列表 |
| /api/user/create | POST | 创建用户 |
| /api/user/update | PUT | 更新用户 |
| /api/user/delete | DELETE | 删除用户 |
| /api/llm/config | GET/PUT | LLM配置获取/更新 |
| /api/log/list | GET | 获取日志列表 |
| /api/carModel/list | GET | 获取车型列表 |
| /api/carModel/config | GET/PUT | 车型配置获取/更新 |
| /api/group/list | GET | 获取群列表 |
| /api/group/config | GET/PUT | 群配置获取/更新 |
| /api/vin/record | GET | 获取车架号记录 |
| /api/statistics/message | GET | 获取消息统计 |

---

## 七、样式规范

### 7.1 CSS变量定义 (styles/variables.css)

```css
:root {
  /* 主题色 */
  --primary-color: #409EFF;
  --primary-light: #ECF5FF;
  --primary-dark: #337ECC;
  
  /* 状态色 */
  --success-color: #67C23A;
  --warning-color: #E6A23C;
  --danger-color: #F56C6C;
  --info-color: #909399;
  
  /* 文字色 */
  --text-primary: #303133;
  --text-regular: #606266;
  --text-secondary: #909399;
  --text-placeholder: #C0C4CC;
  
  /* 边框色 */
  --border-base: #DCDFE6;
  --border-light: #E4E7ED;
  --border-lighter: #EBEEF5;
  --border-extra-light: #F2F6FC;
  
  /* 背景色 */
  --bg-base: #F5F7FA;
  --bg-white: #FFFFFF;
  
  /* 阴影 */
  --shadow-base: 0 2px 4px rgba(0, 0, 0, 0.12);
  --shadow-light: 0 2px 12px 0 rgba(0, 0, 0, 0.1);
  
  /* 圆角 */
  --border-radius-small: 2px;
  --border-radius-base: 4px;
  --border-radius-large: 8px;
  
  /* 间距 */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
}
```

### 7.2 布局规范

- 侧边栏宽度：220px（可折叠为64px）
- 顶部导航高度：60px
- 内容区域边距：20px
- 卡片内边距：20px
- 表格行高：48px

### 7.3 响应式断点

| 断点名 | 宽度范围 | 说明 |
|--------|----------|------|
| xs | < 768px | 手机端 |
| sm | 768px - 991px | 平板端 |
| md | 992px - 1199px | 小型桌面 |
| lg | 1200px - 1919px | 标准桌面 |
| xl | ≥ 1920px | 大屏幕 |

---

## 八、交互规范

### 8.1 表单交互
- 输入框聚焦时显示蓝色边框
- 实时验证，错误即时提示
- 提交按钮带加载状态
- 表单重置需二次确认

### 8.2 表格交互
- 表头支持点击排序
- 行悬停高亮显示
- 操作按钮悬停显示Tooltip
- 批量操作支持多选

### 8.3 弹窗交互
- 点击遮罩可关闭（配置项）
- ESC键可关闭
- 表单弹窗关闭前检查是否有未保存内容
- 删除操作需二次确认

### 8.4 消息提示
- 操作成功：右上角绿色提示，2秒自动消失
- 操作失败：右上角红色提示，需手动关闭
- 重要提示：居中显示，需确认
- 加载状态：全屏或局部Loading

---

## 九、权限控制

### 9.1 路由权限
- 基于角色的路由访问控制
- 未登录用户重定向到登录页
- 无权限页面显示403

### 9.2 按钮权限
- 通过指令控制按钮显示/隐藏
- 权限标识格式：`module:action`
- 示例：`user:create`、`user:delete`

---

## 十、性能优化

### 10.1 加载优化
- 路由懒加载
- 组件异步加载
- 图片懒加载
- 静态资源CDN（可选）

### 10.2 渲染优化
- 大数据表格使用虚拟滚动
- 图表使用Canvas渲染
- 避免不必要的重渲染

### 10.3 缓存策略
- 接口数据合理缓存
- 本地存储用户偏好设置
- 路由Keep-alive缓存

---

## 十一、开发注意事项

1. **CSS必须使用本地文件**，禁止引用外部CDN资源
2. 所有组件需有明确的Props和Events定义
3. TypeScript类型定义需完整，避免使用any
4. 接口请求统一封装，统一错误处理
5. 表单验证规则需与后端保持一致
6. 页面标题需动态设置
7. 关键操作需有确认提示
8. 敏感信息（如密码）需脱敏显示
9. 日志等大数据量页面需支持导出功能
10. 所有页面需适配主流浏览器（Chrome、Firefox、Edge、Safari）

---

## 十二、文件命名规范

- 组件文件：PascalCase，如 `UserManage.vue`
- 页面文件：index.vue 或 功能名.vue
- 工具文件：camelCase，如 `formatDate.ts`
- 样式文件：kebab-case，如 `user-manage.css`
- 常量文件：UPPER_SNAKE_CASE

---

*文档生成时间：2026-04-17*
*版本：v1.0*
